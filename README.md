# employment-seam
Pattern Commons #7. The boundary event when a person enters or exits an employer–worker relationship. Worker owns the knowledge graph; the platform facilitates the handoff and exits. Spec-only—the first entry in the local-first series without a prototype.
