import type { Message } from "@automerge/automerge-repo/slim";
import type { Keyhive } from "@keyhive/keyhive/slim";
import type { KeyhiveMessageData, KeyhiveMessage } from "./messages.js";
import { Metrics } from "./metrics.js";
export declare class MessageBatch {
    readonly messages: KeyhiveMessage[];
    readonly metrics: Metrics;
    private readonly syncRequestSenders;
    add(msg: Message, data: KeyhiveMessageData): void;
    countNonKeyhive(): void;
    get isEmpty(): boolean;
}
export declare class BatchProcessor {
    private readonly batchInterval;
    private readonly keyhive;
    private readonly handleMessage;
    private readonly swapBatch;
    private timeoutId?;
    constructor(batchInterval: number, keyhive: Keyhive, handleMessage: (msg: Message, data: KeyhiveMessageData, metrics: Metrics) => Promise<void>, swapBatch: () => MessageBatch);
    start(): void;
    stop(): void;
    private scheduleNext;
    private processAndReschedule;
    private processBatch;
}
//# sourceMappingURL=batch.d.ts.map