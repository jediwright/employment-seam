import { type BlobInterceptor, type DocumentId, type StorageAdapterInterface } from "@automerge/automerge-repo/slim";
import { Keyhive } from "@keyhive/keyhive/slim";
import { PromiseQueue } from "../network-adapter/pending.js";
export declare class KeyhiveBlobInterceptor implements BlobInterceptor {
    #private;
    constructor(keyhive: Keyhive, queue: PromiseQueue, onEncrypted?: () => void, storage?: StorageAdapterInterface);
    loadPersistedPcsKeyHashes(): Promise<void>;
    get trackedDocIds(): string[];
    get docIdsAwaitingPcsKey(): string[];
    lastPcsKeyHashForDoc(documentId: string): Uint8Array | undefined;
    transformOutgoing(documentId: DocumentId, commitId: string, parents: string[], blob: Uint8Array, loadBlob?: (commitIdHex: string) => Promise<Uint8Array | null>): Promise<Uint8Array | null>;
    transformIncoming(documentId: DocumentId, commitId: string, blob: Uint8Array, loadBlob?: (commitIdHex: string) => Promise<Uint8Array | null>): Promise<Uint8Array | null>;
}
//# sourceMappingURL=blob-interceptor.d.ts.map