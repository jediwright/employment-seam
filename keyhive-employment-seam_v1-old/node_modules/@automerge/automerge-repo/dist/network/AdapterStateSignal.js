export class AdapterStateSignal {
    currentValue;
    #nextPromise;
    #resolveNext = () => { };
    #rejectNext = () => { };
    #done = false;
    constructor(currentValue) {
        this.currentValue = currentValue;
        this.#nextPromise = new Promise((resolve, reject) => {
            this.#resolveNext = resolve;
            this.#rejectNext = reject;
        });
    }
    async *watch() {
        while (!this.#done) {
            try {
                yield await this.#nextPromise;
            }
            catch (e) {
                // stopped - exit cleanly
            }
        }
    }
    get value() {
        return this.currentValue;
    }
    set(value) {
        if (this.#done)
            return;
        this.currentValue = value;
        const lastResolve = this.#resolveNext;
        this.#nextPromise = new Promise((resolve, reject) => {
            this.#resolveNext = resolve;
            this.#rejectNext = reject;
        });
        // Prevent re-entrancy
        queueMicrotask(() => lastResolve(value));
    }
    stop() {
        this.#done = true;
        this.#rejectNext(new Error("stopped"));
    }
}
//# sourceMappingURL=AdapterStateSignal.js.map