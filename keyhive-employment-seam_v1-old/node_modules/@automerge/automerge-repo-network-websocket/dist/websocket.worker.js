/// <reference lib="webworker" />
/** Worker entrypoint for {@link WebSocketWorkerClientAdapter}. */
import { makeWebSocketWorkerHandler, } from "./websocket-worker-handler.js";
import { WS_WORKER_RPC, } from "./websocket-worker-rpc.js";
const handle = makeWebSocketWorkerHandler({
    createSocket: (url) => new WebSocket(url),
    post: (event) => self.postMessage({
        channel: WS_WORKER_RPC,
        ...event,
    }),
});
self.onmessage = (e) => handle(e.data);
//# sourceMappingURL=websocket.worker.js.map