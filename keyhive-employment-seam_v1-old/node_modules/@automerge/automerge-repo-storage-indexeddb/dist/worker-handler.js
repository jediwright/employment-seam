/**
 * The {@link STORAGE_RPC} dispatcher behind `worker.ts`, factored out so it can
 * be tested without a browser. Hosts {@link IndexedDBStorageAdapter}s keyed by
 * the request `client`.
 */
import { IndexedDBStorageAdapter } from "./index.js";
import { STORAGE_RPC, } from "./worker-rpc.js";
/** The `ArrayBuffer`s in a read result, to transfer back instead of copy. */
export function collectTransfer(method, result) {
    const buffers = new Set();
    const add = (buffer) => {
        if (buffer instanceof ArrayBuffer)
            buffers.add(buffer);
    };
    if (method === "load") {
        if (result instanceof Uint8Array)
            add(result.buffer);
    }
    else if (method === "loadRange") {
        for (const chunk of result ?? []) {
            if (chunk?.data instanceof Uint8Array)
                add(chunk.data.buffer);
        }
    }
    return [...buffers];
}
export function makeStorageRpcDispatcher() {
    const adapters = new Map();
    return async function dispatch(msg, reply) {
        if (!msg || msg.channel !== STORAGE_RPC)
            return;
        const { client, id, method, args } = msg;
        try {
            if (method === "init") {
                const [database, store] = args;
                adapters.set(client, new IndexedDBStorageAdapter(database, store));
                reply({ channel: STORAGE_RPC, client, id, ok: true, result: undefined }, []);
                return;
            }
            const adapter = adapters.get(client);
            if (!adapter)
                throw new Error("IndexedDB worker storage not initialized");
            const fn = adapter[method];
            const result = await fn.apply(adapter, args);
            reply({ channel: STORAGE_RPC, client, id, ok: true, result }, collectTransfer(method, result));
        }
        catch (err) {
            reply({
                channel: STORAGE_RPC,
                client,
                id,
                ok: false,
                error: err instanceof Error ? err.message : String(err),
            }, []);
        }
    };
}
//# sourceMappingURL=worker-handler.js.map