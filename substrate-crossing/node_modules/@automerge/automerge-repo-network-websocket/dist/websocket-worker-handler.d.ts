import { type WsWorkerCommand, type WsWorkerEventBody } from "./websocket-worker-rpc.js";
/** The minimal slice of `WebSocket` this needs (so tests can fake it). */
export interface WsSocketLike {
    binaryType: string;
    send(data: ArrayBuffer): void;
    close(): void;
    addEventListener(type: string, listener: (event: {
        data?: unknown;
    }) => void): void;
    removeEventListener(type: string, listener: (event: {
        data?: unknown;
    }) => void): void;
}
export interface WebSocketWorkerHandlerOptions {
    createSocket: (url: string) => WsSocketLike;
    post: (event: WsWorkerEventBody) => void;
}
/** Returns a handler for inbound {@link WsWorkerCommand}s. */
export declare function makeWebSocketWorkerHandler({ createSocket, post, }: WebSocketWorkerHandlerOptions): (command: WsWorkerCommand) => void;
//# sourceMappingURL=websocket-worker-handler.d.ts.map