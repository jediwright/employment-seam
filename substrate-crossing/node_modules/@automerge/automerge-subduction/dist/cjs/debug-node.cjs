const bindings = require('./debug-web-bindings.cjs');
const fs = require('fs');
const path = require('path');
bindings.initSync({ module: fs.readFileSync(path.join(__dirname, '../wasm_bindgen/web-debug/automerge_subduction_wasm_bg.wasm')) });
module.exports = bindings;
