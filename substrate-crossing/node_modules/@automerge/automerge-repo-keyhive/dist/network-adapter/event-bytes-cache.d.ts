import { Identifier, Keyhive } from "@keyhive/keyhive/slim";
export declare class EventBytesCache {
    private entries;
    getBytesFor(hashStrings: Set<string>): {
        events: Uint8Array[];
        cborEvents: Uint8Array[];
        missingHashes: Set<string>;
    };
    storeAndCollect(wasmEvents: Map<Uint8Array, Uint8Array>, missingHashes: Set<string>, events: Uint8Array[], cborEvents: Uint8Array[]): void;
}
export declare function fetchAgentAndPublicEvents(keyhive: Keyhive, agentId: Identifier): Promise<Map<Uint8Array, Uint8Array>>;
//# sourceMappingURL=event-bytes-cache.d.ts.map